{:paths ["bb"]
 :tasks
 {:requires ([babashka.fs :as fs]
             [clojure.edn :as edn]
             [tasks :as tasks])
  :init
  (do
    (defn shelll [& args]
      (println args)
      (apply shell args)))

  build (shelll "echo 'TODO impl building'")
  watch (shelll "echo 'TODO impl watching'")}}
